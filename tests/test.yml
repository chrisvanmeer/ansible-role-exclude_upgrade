---
- name: Test
  hosts: localhost
  become: true

  roles:
    - role: ansible-role-exclude_upgrade
      exclude_upgrade_packages:
        - consul-enterprise
        - vault-enterprise
        - nomad-enterprise
